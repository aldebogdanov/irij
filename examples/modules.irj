mod myapp.auth.jwt

use std.io
use std.json :open
use std.http {get post}

pub fn authenticate :: Request -[Http]-> Result Token Error
  token := get "/api/token"
  match token
    Ok t => Ok t
    Err e => Err e
